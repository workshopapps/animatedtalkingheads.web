import Layout from '../components/UI/Layout';

// <>
//   <button onClick={() => setShow((prevState) => !prevState)}>Chat</button>
//   <ChatBox isChatBoxShowing={show} setIsChatBoxShowing={setShow} />
// </>

const Home = () => {
  return (
    <Layout>
    </Layout>
  );
};

export default Home;
